zerops:
  - setup: api
    build:
      base: nodejs@22
      buildCommands:
        - cd api && npm install && npx prisma generate --schema=../prisma/schema.prisma
      deployFiles:
        - api
        - prisma
        - node_modules
    run:
      base: nodejs@22
      ports:
        - port: 3000
          httpSupport: true
      start: cd api && npm run start:api
      envVariables:
        DATABASE_URL: ${db_connectionString}
        REDIS_URL: ${cache_connectionString}

  - setup: worker
    build:
      base: nodejs@22
      buildCommands:
        - cd worker && npm install && npx prisma generate --schema=../prisma/schema.prisma
      deployFiles:
        - worker
        - prisma
        - node_modules
    run:
      base: nodejs@22
      start: cd worker && npm run start:worker
      envVariables:
        DATABASE_URL: ${db_connectionString}
        REDIS_URL: ${cache_connectionString}

  - setup: frontend
    build:
      base: nodejs@22
      buildCommands:
        - echo "DEBUG RUNTIME_FRONTEND_API_URL is [$RUNTIME_FRONTEND_API_URL]"
        - cd frontend && VITE_API_URL=$RUNTIME_FRONTEND_API_URL npm run build
      deployFiles:
        - frontend/dist/~
    run:
      base: static